name: Build and Deploy

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        
    - name: Install dependencies
      run: npm install
      
    - name: Build minified version
      run: npm run build
      
    - name: Create deployment directory
      run: |
        mkdir -p dist
        mkdir -p dist/demos
        cp vdoninja-sdk.min.js dist/
        cp vdoninja-sdk.js dist/
        cp vdoninja-sdk-node.js dist/
        cp webrtc-adapter.js dist/
        cp index.html dist/
        cp style.css dist/
        cp -r demos/*.html dist/demos/
        cp -r demos/*.js dist/demos/
        cp -r demos/*.md dist/demos/
        cp README.md dist/
        cp README-NODE.md dist/
        cp AI-INTEGRATION.md dist/
        
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    
    steps:
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4